const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'test',
    USERNAME: 'root',
    PASSWORD: '854275944618',
    PORT: '3306',
    HOST: '127.0.0.1'
  }
}

module.exports = config