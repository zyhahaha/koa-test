const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'koa_test',
    USERNAME: 'root',
    PASSWORD: '854275944618',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config